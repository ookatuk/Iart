default handler.
**std and `enable-default-handler` required.**

# Behavior

* When an item is dropped without being used(`DroppedWithoutCheck`),
  `eprintln`(stderr) is executed.
* If raised Others, Does nothing.