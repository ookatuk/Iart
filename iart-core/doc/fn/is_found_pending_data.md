Returns `true` if the tracker is tracking something.

If `enable-pending-tracker-tracking-count` is disabled,
the execution speed is `O(n)`.

If `enable-pending-tracker-tracking-count` is enabled, the speed is equivalent to Atomic.